

<body>
    <div class="container-fluid">
        <div class="sub-container thehome">
            <div class="p-3">
                <h1 class="thenewarrival"> Users Ordered Products</h1>
                <div class="theArea">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">Address</th>
                                <th scope="col">Product name</th>
                                <th scope="col">Quantity</th>
                                <th scope="col">Price</th>
                                
                                
                            </tr>
                        </thead>
                        <tbody>
                            {{#each order}}

                            {{#each product}}
                            <tr>
                                <td>{{../addressId}}</td>
                                <td>{{name}}</td>
                                <td>{{quantity}}</td>
                                <td>₹{{price}}</td>            
                            </tr>
                            {{/each}}

                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</body>